{%set build = 0 %}
{%set rev = '4addff7' %}
{%set req = {'openmpi':'>=1.8.5'} %}
{%set version = '2.0.0' %}

package:
  name: mpi4py
  version: {{version}}.git.{{rev}}

about:
  home: https://bitbucket.org/mpi4py/
  license: 'BSD (https://bitbucket.org/mpi4py/mpi4py/src/master/LICENSE.rst)'
  summary: 'Python bindings for MPI.'

source:
  git_url: https://bitbucket.org/mpi4py/mpi4py
  git_rev: {{rev}}

requirements:
  build:
    - python
    - cython >=0.22
    - openmpi {{req[mpi]}}

  run:
    - python
    - openmpi {{req[mpi]}}

build:
  number: {{build}}
  string: py{{py}}_openmpi185_{{build}}
  script: python setup.py build_exe install

